import{Link}from"react-router-dom";import{useDeals}from"../hooks/useDeals";import{useShoppingList}from"../hooks/useShoppingList";import{compareStoreOffers}from"../lib/compare";import{price,stores}from"../lib/format";import{resolveShoppingItem}from"../lib/shopping-list";import{STORE_ORDER,type Store}from"../types";export function ShoppingList(){const{deals,loading}=useDeals(),{items,remove,clear}=useShoppingList(),resolved=items.map(item=>resolveShoppingItem(item,deals)),counts=Object.fromEntries(STORE_ORDER.map(store=>[store,0])) as Record<Store,number>;let equal=0,single=0;for(const row of resolved){if(!row.deal)continue;const comparison=compareStoreOffers(row.deal.offers);if(comparison.winner)counts[comparison.winner]++;else if(comparison.equal)equal++;else single++}if(loading)return <div className="state">Loading your shopping list…</div>;return <main className="page"><div className="page-heading split-heading"><div><p className="kicker">YOUR SHOPPING LIST</p><h1>{items.length} product{items.length===1?"":"s"}</h1><p>Current tracked promotions only. No estimated shelf prices.</p></div>{items.length>0&&<button className="clear-list" onClick={()=>window.confirm("Clear your shopping list?")&&clear()}>Clear list</button>}</div>{items.length===0?<div className="state">Your shopping list is empty.<br/><Link className="text-link" to="/">Add products from current deals.</Link></div>:<><section className="list-summary">{STORE_ORDER.map(store=><div key={store}><strong>{stores[store].label}</strong><span>Cheapest for {counts[store]} item{counts[store]===1?"":"s"}</span></div>)}<div><strong>{equal}</strong><span>same-price comparisons</span></div><div><strong>{single}</strong><span>not cross-store comparable</span></div></section><div className="shopping-items">{resolved.map(({item,deal})=>{const comparison=deal?compareStoreOffers(deal.offers):undefined;return <article className="shopping-item" key={item.id}><div className="shopping-item-head"><div><span className="eyebrow">SAVED PRODUCT</span><h2>{deal?<Link to={`/products/${deal.id}`}>{deal.name}</Link>:item.label}</h2></div><button onClick={()=>remove(item.id)}>Remove</button></div>{deal?<div className="shopping-prices">{STORE_ORDER.map(store=><div key={store}><span className="store" style={{color:stores[store].color}}>{stores[store].label}</span><strong>{comparison?.offers[store]?price(comparison.offers[store]!):"No tracked promotion"}</strong></div>)}</div>:<p className="muted">Unavailable in current deals.</p>}{comparison?.winner&&<p className="comparison-result">{stores[comparison.winner].label} is ${comparison.difference?.toFixed(2)}/{comparison.unit} cheaper than the next comparable offer.</p>}{comparison?.equal&&<p className="comparison-result">Same lowest tracked unit price.</p>}</article>})}</div></>}</main>}
