import{describe,expect,it}from"vitest";import{parseAdvertisedUnitPrice,parseQuantity}from"../scripts/normalization/normalize-units.js";describe("AU quantity normalization",()=>{it("normalizes multipacks",()=>{expect(parseQuantity("Coke 10x375mL | 3.75L")?.baseQuantity).toBe(3.75)});it("normalizes per 100g prices",()=>{expect(parseAdvertisedUnitPrice("$2.72/ 100g")).toEqual({price:27.2,type:"kg"})})});
