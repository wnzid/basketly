const DEFAULT_HEADERS={"user-agent":"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/142 Safari/537.36 BasketlyAU/1.0",accept:"text/html,application/xhtml+xml,application/json;q=0.9,*/*;q=0.8","accept-language":"en-AU,en;q=0.9"};
async function request(url:string,init:RequestInit={},attempts=3){let last:unknown;for(let attempt=1;attempt<=attempts;attempt++)try{const response=await fetch(url,{...init,redirect:"follow",signal:AbortSignal.timeout(25000),headers:{...DEFAULT_HEADERS,...(init.headers??{})}});if(response.ok)return response;if(response.status<500&&response.status!==429)throw new Error(`${response.status} ${response.statusText}`);last=new Error(`${response.status} ${response.statusText}`)}catch(error){last=error;if(attempt<attempts)await new Promise(resolve=>setTimeout(resolve,800*attempt))}throw new Error(`Failed to fetch ${url}: ${last instanceof Error?last.message:String(last)}`)}
export async function fetchText(url:string,init?:RequestInit){return(await request(url,init)).text()}
export async function fetchJson<T>(url:string,init?:RequestInit){return(await request(url,{...init,headers:{accept:"application/json",...(init?.headers??{})}})).json() as Promise<T>}
